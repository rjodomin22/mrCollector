<form method="post" action="{{ path('app_maker_delete', {'id': maker.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maker.id) }}">
    <button class="btn">Delete</button>
</form>
